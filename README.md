#  CipherDash 

CipherDash is a small web-based puzzle game (hackathon MVP).
Players encrypt short plaintext signals by composing cipher nodes.

Run locally: `python -m http.server 8000` and open `http://localhost:8000`

See the project files for implementation details.
